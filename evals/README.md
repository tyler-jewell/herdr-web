# herdr-web compliance evals

Max 10. Not challenge tasks — policy guards for agents editing this product.

```bash
./scripts/evals.sh list
./scripts/evals.sh run
```

As Herdr plugin actions: `evals-list`, `evals-run`.
