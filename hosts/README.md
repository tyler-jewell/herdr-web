# hosts/

This directory is **intentionally empty** of host registry content.

herdr-web is a portable Integrations UI for any Herdr install.  
Multi-host inventory and AI-first setup live in the separate **tyler-jewell** methodology repo, not here.

Only `.gitkeep` is tracked so the folder exists in clones.
